#!/bin/sh
# AppRun — entry point invoked when the AppImage is executed.
# AppImage automatically sets $APPDIR to the mount point.

HERE="$(dirname "$(readlink -f "${0}")")"
export PATH="${HERE}/usr/bin:${PATH}"
exec "${HERE}/usr/bin/claudepanel" "$@"
